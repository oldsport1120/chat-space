.side-bar
  .side-bar__user-name
    .side-bar__user-name__side-box
      %p.side-bar__user-name__side-box__name
        = current_user.name
      %ul.side-bar__user-name__side-box__menu
        %li.side-bar__user-name__side-box__menu__new
          = link_to new_group_path do
            = icon('fas', 'pen-square', class: 'icon')
        %li.side-bar__user-name__side-box__menu__cog
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: 'icon')
  .group-list
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          %p.group-list__group__group-name
            = group.name
          %p.group-list__group__group-message
            まだメッセージがありません